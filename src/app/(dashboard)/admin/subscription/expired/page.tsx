// =============================================================
// FILE: src/app/(dashboard)/admin/subscription/expired/page.tsx
import { redirect } from 'next/navigation'

export default function ExpiredPage() {
    redirect('/admin/subscription')
}